import { HeroPool } from "my-v0-project"

// HeroPool is a full-bleed marketing section that takes no props — the honest
// preview is the section itself, on the page background it is designed for.
export function Section() {
  return (
    <div className="bg-cloud">
      <HeroPool />
    </div>
  )
}
